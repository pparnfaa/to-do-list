<div class="max-w-md mx-auto mt-10 p-6 bg-white shadow-md rounded-2xl">
  <!-- Notice -->
  <!-- Quest Info -->
  <div class="mb-6">
    <h2 class="text-xl font-semibold text-indigo-700 mb-2">Quest Detail</h2>
    <p class="text-gray-700">
      <span class="font-medium">Name:</span> <%= @quest.name %>
    </p>
    <p class="mt-2">
      <span class="font-medium">Status:</span>
      <% if @quest.status %>
        <span class="text-green-600 font-semibold">Complete ✅</span>
      <% else %>
        <span class="text-gray-500 italic">in process⏳</span>
      <% end %>
    </p>
  </div>
  <!-- Action Buttons -->
  <div class="flex items-center justify-between space-x-2">
    <%= link_to "✏️ Edit",
        edit_quest_path(@quest),
        class: "flex-1 text-center bg-indigo-400 hover:bg-indigo-500 text-white px-4 py-2 rounded-full shadow-sm" %>
    <%= link_to "⬅️ Back",
        quests_path,
        class: "flex-1 text-center bg-gray-200 hover:bg-gray-300 text-gray-700 px-4 py-2 rounded-full shadow-sm" %>
    <%= button_to "🗑 Delete",
        @quest,
        method: :delete,
        data: { confirm: "Are you sure?" },
        class: "flex-1 text-center bg-red-400 hover:bg-red-500 text-white px-4 py-2 rounded-full shadow-sm" %>
  </div>
</div>
